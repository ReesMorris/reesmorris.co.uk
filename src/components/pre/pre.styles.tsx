import styled from '@emotion/styled';

const Pre = styled.pre`
  position: relative;
  padding: 1.6rem;
  line-height: 1.45;
  overflow: auto;
  margin: 0 0 1.6rem 0;

  > code {
    font-size: 1.4rem;
  }
`;

export default { Pre };
